import { atom } from "recoil";

export const darkModeState = atom({
    key: 'darkModeState',
    default: false
})

export const loadingState = atom({
    key: "loadingState",
    default: false
})

